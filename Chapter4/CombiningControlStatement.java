//Sequencing: Put one control statement after another. The first one finishes completely, then the second one starts. They run independently, one after the other.
//Nesting: Put one control statement inside another. Example: an if inside a while, or a while inside an if. The inner one only runs when the outer one allows it to. You can nest as deep as you need.
